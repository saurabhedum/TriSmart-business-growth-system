import express from 'express';

export const webhooksRouter = express.Router();

// This router will house the WhatsApp, Instagram, and Payment Webhooks.
// (Transitioning code from server.ts in subsequent steps)
