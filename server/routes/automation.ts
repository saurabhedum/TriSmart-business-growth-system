import express from 'express';

export const automationRouter = express.Router();

// This router will house the Cron Jobs, BullMQ queues, and Broadcast automation.
// (Transitioning code from server.ts in subsequent steps)
