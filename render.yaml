services:
  - type: web
    name: smartbilling-app
    env: node
    plan: free 
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3000
